// Site-wide settings that aren't finalized yet.
//
// joinUrl: set this once the club has an official signup form / Discord / GroupMe link.
//   Until then, the Join button renders as a disabled state so nothing broken ships.
// contactEmail: set once an official club email exists.
export const siteConfig = {
  joinUrl: null,
  contactEmail: null,
}
